# CORS Origins (comma-separated). Use 10721 when frontend runs on reservoir port.
# CORS_ORIGINS=http://localhost:13001
CORS_ORIGINS=http://localhost:10721,http://127.0.0.1:10721

# Use direct import for MCP (false=fast, true=HTTP self-call can hang)
MCP_USE_HTTP=false

# Server Configuration (10720 = reservoir port per mcp-central-docs; 13000 = legacy)
HOST=0.0.0.0
PORT=10720
RELOAD=true
