# UI Components

This directory will contain shadcn/ui components.

To add components:

```bash
npx shadcn@latest add button
npx shadcn@latest add card
npx shadcn@latest add dialog
# etc.
```

See: https://ui.shadcn.com/docs/components
